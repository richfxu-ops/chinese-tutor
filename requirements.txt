# Local deps (Apple Silicon): data generation + eval + Gradio demo.
# Training deps are separate — see requirements-train.txt (Colab/CUDA only).
anthropic>=0.40
transformers>=4.46
torch>=2.4
accelerate>=1.0
gradio>=5.0            # Textbox(submit_btn=...) needs 5.0+
# Local serving of the quantized 7B (ships Metal wheels for Apple Silicon)
llama-cpp-python>=0.3.0
# Reading layer (app-side pinyin + hover gloss)
pypinyin>=0.53
jieba>=0.42
certifi>=2024.0        # get_cedict.py TLS on python.org macOS builds
